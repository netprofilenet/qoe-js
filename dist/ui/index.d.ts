/**
 * QOE Client - UI Helpers
 *
 * Optional UI utilities for visualization and formatting.
 * Requires Chart.js as a peer dependency.
 */
export declare class MetricFormatter {
    static formatBandwidth(bps: number, decimals?: number): string;
    static formatLatency(ms: number, decimals?: number): string;
    static formatPercent(value: number, decimals?: number): string;
    static formatDuration(ms: number): string;
}
//# sourceMappingURL=index.d.ts.map